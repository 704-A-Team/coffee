<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.oracle.coffee.ClientMapper">

	<select id="clientInfo" parameterType="int" resultType="SWClientDto">
		select DISTINCT client_code, client_name
		from client_tb
		where client_type = #{client_type}
	</select>
	
	<select id="getClientsByProduct" parameterType="int" resultType="SWClientDto">
		select *
		from provide p
		join client_tb c 
			ON p.provide_client_code = c.client_code
		where p.product_won_code = #{product_code}
	</select>
	
	<select id="clientIsList" parameterType="SWClientDto" resultType="SWClientDto">
		select 	*
		from 	client_tb c
		where 	client_type = #{client_type}
		and 	client_status = #{client_status}
	</select>

</mapper>